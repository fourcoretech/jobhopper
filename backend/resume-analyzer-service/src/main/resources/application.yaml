server:
  port: 8082

spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  application:
    name: resume-analyzer-service

ws:
  endpoint:
    open-ai-api: "https://api.openai.com/v1/chat/completions"
jwt:
  secret: YOUR_GENERATED_SECRET  # Must match the one in auth-service
openai:
  api-key: YOUR_OPENAI_API_KEY

